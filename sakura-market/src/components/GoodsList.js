import React from "react";

const GoodsList = (props) =>  {
    const {isLogin, user, changeState } = props;
    return (
        <>
            <h4>これはGoodsListです</h4>
        </>
    );
};

export default GoodsList;